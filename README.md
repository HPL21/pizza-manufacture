# Project description

Nice little project
**TODO: add project description** 

# Installation

1. Install Taskfile (https://taskfile.dev/docs/installation) - optional
2. Install docker (if needed)
3. Run `task up` (`docker compose up -d` if you don't have Taskfile installed)